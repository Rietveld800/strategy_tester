# Python environment and caches -- environment-specific / regenerable
venv/
__pycache__/
*.pyc
*.pyo

# Build intermediate for the HTML report (regenerated by quickfix_portfolio.py)
output/_equity_data.json

# The R-cut trade cache: 6 MB of engine output, regenerable by rerunning
# build_1m_rcut_report.py. It exists so a new METRIC costs seconds instead of
# 96 minutes of backtesting -- worth keeping on disk, not worth versioning.
output/quickfix1m1dcRcut_trades.json
